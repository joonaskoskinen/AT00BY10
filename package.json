{
  "name": "at00by10-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "coverage": "c8 --reporter=text --reporter=lcov node --test"
  },
  "devDependencies": {
    "c8": "^10.1.2"
  },
  "c8": {
    "exclude": [
      "src/.internal/**"
    ]
  }
}
